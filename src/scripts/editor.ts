import '../styles/editor.css';

// By default imported in footer, so no need for document.ready.
// If you have no global JS, set css-only to true in corresponding
// Vite\enqueue_asset in WI\StarterFSE\Assets.php
